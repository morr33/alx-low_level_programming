#include "main.h"

/**
 * print_alpahbet - print the alphabet in lowercase
 *
 * Return:void
 */

void print_alphabet(void);
{
	char loa;

	for (loa = 'a'; loa <= 'z'; loa++)
		_putchar(loa);

		_putchar('\n');
}
